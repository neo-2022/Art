# Stream metrics

- `stream_lag_ms` - lag stream-выдачи в миллисекундах.
- `stream_subscriber_count` - число активных подписчиков.
- `stream_disconnect_total` - число разрывов stream-соединений.
- `stream_unavailable_total` - число ошибок `observability_gap.stream_unavailable`.
