# Secure SDLC policy

- clean build в чистом CI окружении
- reproducible build
- lock files обязательны
- повторная релизная сборка из того же коммита должна давать identical artifact hash
- latest теги инструментов в CI запрещены
