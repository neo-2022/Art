# История, Концепции И Уникальные Механики Art

## Source of truth
- `docs/source/Art_v1_spec_final.md`
- `docs/source/FOUNDATION_CONSTITUTION_V0_2.md`
- `docs/source/checklists/CHECKLIST_00_MASTER_ART_REGART.md`
- `docs/testing/defect_remediation_ladder_v0_2.md`
- `formats/root_decision_tree_dependencies.yaml`
- `docs/foundation/revolutionary_hypotheses.md`
- `docs/foundation/frontier_tech_radar.md`
- `docs/foundation/lens_audit_report.md`

Последняя актуализация: 2026-03-06
Статус: ACTIVE

## 0. Роль в корне дерева решений

Этот документ является вторым по приоритету корневым документом проекта.

Он отвечает за:
- исторический корпус замысла;
- утверждённые differentiators;
- смысловую непрерывность проекта между стадиями, аудитом и remediation.

Он не задаёт process law и не подменяет конституцию. Если возникает конфликт между историческим корпусом и обязательным execution-law v0.2, приоритет имеет:
1. `docs/source/FOUNDATION_CONSTITUTION_V0_2.md`
2. `docs/testing/defect_remediation_ladder_v0_2.md`
3. этот документ

Любое изменение этого файла требует синхронного обновления зависимых документов по карте:
- `formats/root_decision_tree_dependencies.yaml`

## 1. Зачем нужен этот документ

Art развивался не как обычный "набор экранов", а как система с большим количеством принципов, экспериментальных идей, архитектурных законов и уникальных продуктовых решений.

Часть этих идей была уже встроена в код, чек-листы и CI-gates. Часть была утверждена как направление развития, но не всегда была потом аккуратно перенесена в единый foundation-контур. Этот документ собирает всё в один корпус, чтобы:
- не потерять замысел проекта;
- показать, чем именно Art отличается от обычной observability-платформы;
- объяснить обычному человеку, что даёт каждая концепция;
- отделить уже внедрённые механики от экспериментальных и будущих;
- не позволить безолаберно "забывать" утверждённые идеи между чатами, документами и кодом.

## 2. Короткая история проекта

### 2.1 Исходная идея

Art задумывался как Incident OS: не просто место, где видно графики, логи и алерты, а система, где расследование инцидента становится доказуемым, повторяемым и инженерно проверяемым.

Базовая мысль проекта звучит так:
- инциденты нельзя качественно расследовать только через красивые панели;
- выводы без доказательств опасны;
- действия без аудита и трассировки неприемлемы;
- деградация интерфейса не должна оставлять инженера без последнего операционного слоя.

Отсюда появились три слоя продукта:
- Tier A: `Panel0` как аварийный встроенный интерфейс;
- Tier B: `Console` как основной рабочий контур расследования;
- Tier C: `SaaS readiness` как заранее заложенная архитектура для мультиарендности, комплаенса и масштаба.

### 2.4 Переход к защитным контурам hostile production

По мере роста проекта стало понятно, что одной observability и даже одного strong governance недостаточно.

Реальная production-среда ведёт себя враждебно:
- перегружает ingress;
- подделывает входной контекст;
- давит на браузерную поверхность;
- пытается использовать удобные, но небезопасные fallback-решения;
- разрушает соответствие между документами и фактическим состоянием системы.

Из этого выросли отдельные защитные контуры:
- ingress/perimeter anti-DDoS;
- trust boundary hardening;
- browser surface hardening;
- documentation tree как ранний датчик неучтённого документного дрейфа.

### 2.2 Переход от "набора функций" к "конституции"

Проект быстро вышел за пределы обычного backlog. Чтобы не потерять качество, в Art были зафиксированы:
- Foundation / Constitution;
- Master checklist и stage ladder;
- machine-readable contracts;
- evidence-ориентированный governance;
- release gates и go/no-go процесс.

Это превратило проект из набора задач в программу инженерного исполнения.

### 2.3 Переход к уникальным концепциям

После фиксации базовой архитектуры проект начал наращивать более редкие и труднокопируемые подходы:
- Evidence-First;
- Event DNA;
- Truth Modes;
- Investigations-as-Code;
- verifyable audit;
- Visual Flow Mode;
- инновационные треки RTP, LRC, NRAC;
- Linux/Docker/Kubernetes/VM production-readiness;
- адаптивный UX, детерминизм, строгий process law.

Часть этих механизмов уже внедрена. Часть остаётся экспериментальной. Часть зафиксирована здесь как утверждённое направление, чтобы не потеряться дальше.

## 3. Главная идея Art простыми словами

Если объяснить Art человеку без глубокого технического опыта, то проект делает следующее:

- собирает технические сигналы о состоянии системы;
- связывает их не в хаос, а в понятные классы проблем;
- показывает не только "что произошло", но и "на чём основан этот вывод";
- не даёт подменять факты прогнозами;
- помогает безопасно принимать решения и выполнять действия;
- сохраняет полную цепочку доказательств, чтобы потом можно было проверить, воспроизвести и объяснить расследование.

То есть Art нужен не только для наблюдения, а для управляемого, доказуемого и повторяемого реагирования.

## 4. Уникальные механики проекта

Ниже перечислены основные приёмы и механики. Для каждого указано, что он даёт проекту и в каком состоянии находится.

Легенда состояния:
- `Внедрено` — зафиксировано в foundation/checklists и имеет код, тесты или evidence.
- `Эксперимент` — идея встроена в проект, но пока не mandatory и проходит как controlled track.
- `Утверждено, но недоведено` — идея была одобрена, но не получила полного контура contracts/checklists/tests.

### 4.1 Evidence-First
Состояние: `Внедрено`

Что это такое:
- система не должна показывать важное утверждение без ссылки на доказательства.

Что это даёт обычному человеку:
- интерфейсу можно доверять сильнее, потому что он обязан показать, откуда взялся вывод;
- уменьшается риск "магии" и ложной уверенности;
- любая гипотеза или рекомендация раскрывается до реальных данных.

Почему это уникально:
- большинство систем разрешают показывать AI-выводы или агрегаты без строгого доказательного контура;
- Art делает доказательство не украшением, а законом рендера.

### 4.1A Trust Boundary Hardening
Состояние: `Утверждено, материализуется`

Что это такое:
- жёсткий контур, который запрещает системе принимать привилегированный actor-context из недоверенного клиента.

Что это даёт:
- невозможно честно “поднять себе роль” подделкой заголовка;
- audit не превращается в ложную историю, созданную клиентом;
- безопасные действия опираются на доказанный источник доверия.

Почему это важно:
- без этого система выглядит защищённой только на бумаге;
- hostile production всегда будет проверять именно такие места.

### 4.1B Browser Surface Hardening
Состояние: `Утверждено, материализуется`

Что это такое:
- обязательный protective baseline для Browser Level0, Panel0, Console и showcase-слоя:
  - CSP;
  - frame restrictions;
  - browser security headers;
  - integrity и provenance ассетов.

Что это даёт:
- браузерная часть не становится самым слабым местом проекта;
- showcase/demo слой не ломает боевой security baseline;
- легче безопасно выпускать internet-exposed profile.

Почему это важно:
- очень часто сильный backend соседствует со слабой browser surface;
- Art должен быть цельным и в этом месте тоже.

### 4.1C REGART как внешний hostile proving ground
Состояние: `Утверждено, материализуется`

Что это такое:
- использование `REGART` не только как интегрируемой системы, но и как внешнего боевого полигона для проверки `Art`;
- отдельный pinned harness, который воспроизводит:
  - backend события;
  - browser/runtime сигналы;
  - bridge-разрывы;
  - длинные incident-цепочки;
  - audit-mediated действия.

Что это даёт:
- проверка `Art` на реальной внешней системе, а не только на лабораторных fixtures;
- усиление hostile-path coverage без подмены базовых тестов `Art`;
- одновременная польза для двух продуктов: слабости `Art` и `REGART` видны в одном контуре.

Почему это важно:
- интеграции очень часто выглядят “зелёными” только потому, что тестируются на локальном happy-path;
- pinned external harness делает такие ложные зелёные результаты заметными;
- для due diligence это доказывает, что продукт способен выдерживать не только внутреннюю, но и внешнюю среду.

### 4.1D Connected System Visibility
Состояние: `Утверждено, материализуется`

Что это такое:
- отдельный контур, который заставляет Art после подключения внешней системы показывать человеку не абстрактный “зелёный коннектор”, а полную карточку этой системы;
- карточка должна объяснять:
  - что это за система;
  - каким способом она подключена;
  - какие типы данных она обещает отдавать;
  - какие типы данных реально уже пришли;
  - есть ли gap-события и проблемы покрытия.

Что это даёт:
- оператор сразу понимает, подключение реально состоялось или только “формально настроено”;
- исчезает ложное чувство безопасности “pack установлен, значит всё работает”;
- расхождение между обещанным и фактическим покрытием становится видимым, а не скрытым;
- pack, receiver и Console перестают жить отдельной жизнью и начинают говорить про одну и ту же систему одним языком.

Почему это важно:
- большие интеграции чаще всего ломаются не в момент установки, а в момент первого реального потока данных;
- без такого контура внешняя система может считаться подключённой, хотя оператор не видит ни одного свежего сигнала;
- для buyer due diligence это сильный сигнал зрелости: продукт не просто “умеет подключаться”, а умеет честно показывать состояние и ценность подключения.

### 4.2 Event DNA
Состояние: `Внедрено`

Что это такое:
- механизм, который группирует события не по визуальной близости, а по устойчивому "классу проблемы".

Что это даёт:
- инженер видит не тысячи однотипных событий, а повторяющийся паттерн поломки;
- быстрее находится похожий инцидент;
- проще оценить, это новый тип проблемы или уже знакомый.

Почему это важно:
- обычные панели застревают в уровне отдельных логов/алертов;
- DNA делает навигацию по проблемам, а не по шуму.

### 4.3 Truth Modes: Observed / Derived / Predicted
Состояние: `Внедрено`

Что это такое:
- каждый объект в системе обязан явно показывать, что он собой представляет:
  - `Observed` — факт;
  - `Derived` — вычисленный вывод;
  - `Predicted` — прогноз.

Что это даёт:
- человек не путает реальность с расчётом и не принимает прогноз за факт;
- система становится честнее и понятнее;
- резко снижается риск опасных решений на основе предположений.

Почему это уникально:
- многие продукты визуально смешивают факты, агрегаты и предсказания;
- Art делает различие обязательной частью UI и контракта данных.

### 4.4 Tooltip everywhere
Состояние: `Внедрено`

Что это такое:
- любой интерактивный элемент обязан объяснять, что он делает.

Что это даёт:
- продукт проще осваивать;
- интерфейс меньше пугает и реже требует отдельного обучения;
- снижается когнитивная нагрузка в стрессовой ситуации.

### 4.5 One-click-to-evidence
Состояние: `Внедрено`

Что это такое:
- из любого важного объекта можно одним действием перейти к первичному evidence.

Что это даёт:
- человек не теряется в слоях интерфейса;
- можно быстро проверить, не врёт ли агрегат или рекомендация;
- расследование идёт от вывода обратно к доказательству без разрывов.

### 4.6 Panel0
Состояние: `Внедрено`

Что это такое:
- встроенный аварийный интерфейс, который живёт в Core и не зависит от внешнего продуктового UI.

Что это даёт:
- если основной интерфейс упал, инженер не остаётся без управления;
- можно хотя бы увидеть базовое состояние, признаки отказа и последние сигналы;
- это последняя линия устойчивости продукта.

### 4.7 Investigations-as-Code
Состояние: `Внедрено`

Что это такое:
- расследование хранится не как устный контекст и не как чат, а как управляемый документ с lineage, evidence, решениями и результатами.

Что это даёт:
- расследование можно повторить;
- можно сравнить версии и ветки;
- знание не исчезает вместе с конкретным инженером.

### 4.8 Investigation Library
Состояние: `Внедрено`

Что это такое:
- управляемая поверхность для хранения, просмотра, импорта, экспорта, проверки и replay InvestigationDoc.

Что это даёт:
- расследования превращаются в библиотеку практики и корпоративной памяти;
- проще проводить RCA, обучение и аудит;
- появляется реальный reuse знаний.

### 4.9 Dialogic Protocol
Состояние: `Внедрено`

Что это такое:
- вместо свободного чата используются типизированные сущности: `Hypothesis`, `Decision`, `ActionRequest`, `ActionResult`, `Explanation`.

Что это даёт:
- расследование становится структурированным;
- легче проверять, что было гипотезой, что решением, а что результатом действия;

### 4.10A Documentation Tree
Состояние: `Внедрено`

Что это такое:
- графическое и machine-readable дерево документации, которое строится от единственного корня `README.md`.

Что это даёт:
- помогает быстро не потеряться в большом корпусе документов;
- показывает реальные зависимости между документами;
- считает количество документов и строк;
- сигнализирует, когда изменение одного документа требует синхронной корректировки зависимого контура.

Почему это важно:
- большие проекты часто разрушаются не только из-за кода, но и из-за тихого рассинхрона документации;
- в Art документация — часть управляющего механизма, поэтому она тоже должна иметь защитный контур.

### 4.10 Ingress Shield / Anti-DDoS Contour
Состояние: `Утверждено, но недоведено`

Что это такое:
- защитный периметр между внешней сетью и `art-core`, который отделяет обычную перегрузку приложения от враждебного трафика.

Что это даёт:
- продукт не путает business burst с сетевой атакой;
- внешняя среда не должна иметь возможность убить систему простым flood;
- SaaS и public-facing deployment получают отдельный класс защиты, а не только `429` внутри приложения.

Почему это важно:
- Art строится как система для hostile production среды;
- если у неё нет отдельного edge/perimeter contour, часть security- и readiness-claims ложная;
- для банка, SaaS, госсистемы или оператора app-level backpressure сам по себе недостаточен.
- снижается риск неформального хаоса.

### 4.10B Storage pressure / disk exhaustion protection
Состояние: `Утверждено, материализуется`

Что это такое:
- контур, который не даёт системе тихо дойти до `disk full`, пока операторы ещё считают её рабочей;
- это защита не только от DDoS, но и от медленного удушения storage из-за долгого роста данных, bugged integration, duplicate flood или runaway backlog.

Что это даёт:
- система заранее видит давление на БД и WAL;
- включается controlled degraded mode до фактического разрушения storage;
- оператор получает явный сигнал, что проблема уже не “нагрузка”, а угроза жизнеспособности хранилища.

Почему это важно:
- многие системы умирают не мгновенно, а через медленное заполнение диска;
- без такого контура любой красивый recovery после corruption может оказаться бесполезным;
- для hostile production это один из обязательных предохранителей.

### 4.10C Startup configuration fail-closed validator
Состояние: `Утверждено, материализуется`

Что это такое:
- механизм, который запрещает старт системы в конфигурации, уже известной как опасная;
- это защита от “мы запустили как-нибудь, а потом будем разбираться”.

Что это даёт:
- unsafe-конфиг не уходит в production по инерции;
- система отказывается от старта сразу, а не после первого внешнего удара;
- ошибки конфигурации становятся наблюдаемыми и воспроизводимыми.

Почему это важно:
- часть production-проблем вообще не должна доходить до runtime;
- fail-closed startup validator дешевле, чем разбирать уже живую аварию;
- это особенно важно для TLS, storage paths, spool paths, queue limits и release-critical security knobs.

### 4.10D Queue integrity / duplicate / anti-loop protection
Состояние: `Утверждено, материализуется`

Что это такое:
- контур защиты очередей, backlog, replay path и bridge path от дублирования, runaway growth и циклов.

Что это даёт:
- одна шумная интеграция не может тихо разнести весь ingestion path;
- duplicate flood отделяется от нормального трафика;
- replay перестаёт быть источником невидимых циклов.

Почему это важно:
- распределённые системы очень часто ломаются не на storage, а на логике очередей;
- при отсутствии anti-loop/duplicate защиты Art может сам стать источником шума и ложных инцидентов;
- для hostile production это не дополнительная опция, а базовая страховка.

### 4.10E Guard self-observability / self-test
Состояние: `Утверждено, материализуется`

Что это такое:
- принцип, по которому каждый важный защитный механизм обязан сам уметь сообщить, что он сломан или деградировал.

Что это даёт:
- исчезает опасная иллюзия “защита есть, значит всё хорошо”;
- можно увидеть отказ самого guard-а, а не только последствия;
- release/stage closure перестают опираться на предохранители, которые на бумаге живы, а в реальности уже нет.

Почему это важно:
- мёртвый guard опаснее отсутствующего, потому что создаёт ложное чувство безопасности;
- для зрелого проекта важно наблюдать не только систему, но и сами механизмы её защиты.

### 4.10F Расширенный набор предохранителей
Состояние: `Утверждено, materialized в корне и стволе; runtime materialization зависит от remediation`

Что это такое:
- следующий слой защитных механизмов, который делает проект не просто "защищённым от одной угрозы", а системно устойчивым к ошибкам архитектуры, эксплуатации, релиза и сопровождения.

В этот слой входят:
- `Action execution safety guard`
  - запрещает выполнять опасное действие только потому, что оно формально валидно;
- `Agent identity / enrollment / relay trust`
  - не даёт неизвестному агенту стать доверенным источником;
- `Release truth enforcement`
  - не даёт релизу, `README` и `GO/NO-GO` врать о реальной готовности;
- `Authenticity / copyright-safe baseline`
  - не даёт проекту впитывать спорные по происхождению сущности;
- `Regulatory claims drift control`
  - не даёт говорить "сертифицировано" там, где доказано только `certified-ready`;
- `Monolith budget guard`
  - защищает от опасного роста ключевых файлов и зависимости от одного человека;
- `Test strength guard`
  - не даёт закрывать критичные контуры на декоративных или слишком слабых тестах;
- `Documentation drift control`
  - не даёт корню, стволу и кроне разъехаться между собой.

Что это даёт:
- проект становится защищённым не только технически, но и организационно, операционно и юридически;
- слабые места не теряются между аудитом, чек-листами и кодом;
- любые важные claims начинают зависеть от проверяемой реальности, а не от намерений.

Почему это важно:
- production ломается не только от сетевых атак, но и от неверных действий, ложных claims, доверия не тем источникам, слабых тестов и разъехавшейся документации;
- этот слой предохранителей делает Art Incident OS не просто функциональной системой, а системой, которую сложнее обмануть, сломать или исказить по смыслу.

### 4.10 Audit + Merkle Verify
Состояние: `Внедрено`

Что это такое:
- действия и критические цепочки проверяются через неизменяемый audit и proof-цепочку.

Что это даёт:
- можно доказать, что действие реально происходило и не было подменено;
- усиливается доверие для enterprise и compliance-контуров;
- проще разбирать спорные ситуации и инциденты.

### 4.11 Visual Flow Mode
Состояние: `Внедрено как архитектурный и UX-контур; часть advanced runtime остаётся незрелой`

Что это такое:
- особый режим интерфейса, где данные, связи, кластеры и инциденты видны как единое пространство потоков и облаков.

Что это даёт:
- человек быстрее схватывает масштаб и форму проблемы;
- проще увидеть связи между сервисами, типами событий и инцидентами;
- можно перейти от плоских таблиц к картине всей системы.

Что делает его особенным:
- он задуман не как декоративная 3D-картинка, а как инженерная проекция той же модели истины;
- должен сохранять inspectability, evidence lineage, snapshot/replay/diff.

### 4.12 Spatial Store
Состояние: `Внедрено как контракт; runtime-реализация пока не доведена до полной зрелости`

Что это такое:
- слой, который хранит layout и состояние пространственной проекции данных.

Что это даёт:
- Flow/2D/3D режимы могут быть воспроизводимыми;
- можно сохранять snapshot состояния визуальной модели;
- появляется база для будущих XR/3D режимов.

Важно:
- сейчас это один из участков, где архитектурный замысел опережает глубину реализации.

### 4.13 Adaptive UX Policy
Состояние: `Частично внедрено`

Что это такое:
- система должна автоматически подстраивать сложность и насыщенность интерфейса под устройство, GPU и бюджет производительности.

Что это даёт:
- продукт остаётся usable на слабых устройствах;
- красивый интерфейс не должен ломать операционную пригодность;
- уменьшается риск лагов и визуального перегруза.

### 4.14 Audio effects framework
Состояние: `Внедрено`

Что это такое:
- в Console заложен управляемый контур звуковых эффектов с legal-safe baseline и возможностью пользовательской замены.

Что это даёт:
- важные сигналы можно не только видеть, но и слышать;
- появляется дополнительный канал восприятия;
- пользователь может настроить интерфейс под себя, не ломая продукт.

Почему это важно:
- это не просто косметика, а часть адаптивности и эргономики.

### 4.15 Semantic design tokens
Состояние: `Внедрено`

Что это такое:
- цвета, состояния, фокус, предупреждения и интерактивные элементы описываются через строгие семантические токены, а не случайные разрозненные цвета.

Что это даёт:
- интерфейс не деградирует с ростом проекта;
- разные разработчики не начинают придумывать "ещё один оранжевый";
- дизайн остаётся управляемым и масштабируемым.

### 4.16 Linux + Docker + Kubernetes + VM readiness
Состояние: `Частично внедрено`

Что это такое:
- продукт проектируется как production-совместимый не только на хосте, но и в контейнерных и виртуальных окружениях.

Что это даёт:
- продукт можно показать вне рабочего ноутбука разработчика;
- легче доказать реальную готовность к эксплуатации;
- появляется понятная траектория от dev к enterprise deployment.

Важно:
- Ubuntu native, Docker runtime и Kubernetes runtime уже входят в execute-gated контур;
- расширенная VM/Linux-матрица пока не доведена до полной execute-готовности.

### 4.17 Design System И Операционный Визуальный Язык
Состояние: `Внедрено частично`

Что это такое:
- не просто набор цветов и кнопок, а строгий язык интерфейса, который делает продукт одновременно красивым, читаемым и пригодным для работы под нагрузкой.

Что это даёт:
- интерфейс не разваливается в визуальный хаос по мере роста команды и числа экранов;
- пользователь быстрее понимает, где тревога, где факт, где прогноз, где просто акцент;
- продукт выглядит цельным, дорогим и профессиональным, а не собранным из случайных компонентов.

Что уже есть:
- тёмно-золотая операционная палитра;
- семантические токены;
- truth overlay;
- operational/cinematic split для 3D;
- high-contrast/reduced-motion контур;
- search-driven settings и profile manager.

Что ещё важно:
- в истории проекта эта идея была шире, чем просто runtime UI: она включала ещё и брендовый визуальный язык для показа проекта вовне.

### 4.18 Operational Dark Gold / Aero Ops
Состояние: `Внедрено частично`

Что это такое:
- визуальная тема продукта, в которой строгий тёмный фон сочетается с благородными золотыми акцентами и не превращается в декоративную \"премиальность ради премиальности\".

Что это даёт:
- у продукта появляется узнаваемый характер;
- Flow/3D режимы выглядят атмосферно, но не ломают читаемость;
- инженер работает не в скучном безликом UI, а в системе с сильной визуальной идентичностью.

Для обычного человека:
- это как разница между обычной панелью управления и кабиной сложной современной системы, где всё выглядит цельно, серьёзно и продуманно.

### 4.19 Product Showcase / Demonstration Layer
Состояние: `Утверждено, но недоведено`

Что это такое:
- отдельный слой показа продукта рынку: демо-сценарии, визуальные walkthrough, объясняющие схемы, история сценария пользователя, анимация DNA и понятная презентация уникальности.

Что это даёт:
- заказчик видит, что продукт живой и работающий, а не только описанный в документах;
- проще объяснить ценность без погружения в десятки технических файлов;
- легче продавать и защищать архитектурные решения перед нетехнической аудиторией.

Почему это важно:
- в проекте уже есть сильный engineering-контур, но внешний product-showcase слой пока заметно слабее.

### 4.20 AI Engineering Operating Model
Состояние: `Утверждено, но недоведено`

Что это такое:
- представление о разработке Art не как о работе одного универсального ИИ, а как о координации нескольких специализированных агентов: архитектора, инженера ядра, инженера API, фронтенд-инженера, тестировщика, документатора.

Что это даёт:
- разные типы задач обрабатываются профильнее и глубже;
- меньше риск, что важные детали потеряются между кодом, схемами, тестами и документами;
- проще внедрять непрерывное улучшение и ретроспективное обучение.

Для обычного человека:
- это похоже не на одного \"умного робота\", а на инженерную команду с разделением ролей, где каждый отвечает за свою зону.

### 4.21 Advanced Automation Backbone
Состояние: `Утверждено, но недоведено`

Что это такое:
- слой автоматизации, который должен поддерживать качество проекта самыми жёсткими и скучными способами: генерация тестов, синхронизация контрактов, обновление документации, контроль drift, повторные проверки, автоматическое выявление нарушений границ.

Что это даёт:
- уменьшается зависимость от человеческой памяти;
- сложный проект не разваливается на несинхронные куски;
- команда тратит меньше времени на рутину и больше на реальные инженерные решения.

### 4.22 eBPF Evidence Linking
Состояние: `Утверждено, но недоведено`

Что это такое:
- идея автоматически связывать evidence не только с логами и API-данными, но и с низкоуровневыми системными следами Linux: системными вызовами, сетевыми соединениями, файловыми операциями.

Что это даёт:
- расследование становится глубже и убедительнее;
- можно быстрее доказать, что именно происходило на уровне исполнения системы;
- Art получает дифференциатор, которого почти нет у обычных observability-продуктов.

Для обычного человека:
- это как если бы система показывала не только слова о проблеме, но и реальные \"отпечатки пальцев\" происходивших операций.

### 4.23 Self-Healing Tests И Docs
Состояние: `Утверждено, но недоведено`

Что это такое:
- подход, при котором изменения контрактов и API автоматически подталкивают обновление тестов и документации, а не оставляют после себя шлейф ручных поломок.

Что это даёт:
- меньше ложных падений из-за устаревших тестов;
- документация стареет медленнее;
- эволюция API становится менее болезненной.

### 4.24 Wasm Sandbox Для Действий
Состояние: `Утверждено, но недоведено`

Что это такое:
- идея выполнять расширяемые пользовательские действия в безопасной песочнице WebAssembly, а не доверять произвольному коду.

Что это даёт:
- усиливает безопасность Action Studio;
- открывает путь к расширяемости без прямого пробития границ runtime;
- даёт платформенный путь для будущих action-extensions.

### 4.25 Graph-Based DNA Exploration
Состояние: `Утверждено, но недоведено`

Что это такое:
- идея использовать графовые модели для хранения и исследования связей между DNA-кластерами, evidence, инцидентами и действиями.

Что это даёт:
- более естественный поиск похожих цепочек и зависимостей;
- усиливает Flow/relationship exploration;
- может стать основой для более сильной навигации по причинным связям.

### 4.26 Post-Quantum Evidence Signing
Состояние: `Deferred`

Что это такое:
- перспектива использовать постквантовые алгоритмы подписи для долгоживущих audit/evidence артефактов.

Что это даёт:
- повышает долгосрочную устойчивость доверенного следа;
- особенно интересно для высокорегулируемых и долгих контуров хранения.

Сейчас это не ближайшая необходимость, но как differentiator идея ценная.

### 4.27 Графическое дерево документации и контроль дрейфа
Состояние: `Внедрено`

Что это такое:
- специальный навигационный и контрольный механизм, который строит дерево документации от одного корня: `README.md` в корне репозитория;
- каждая вершина дерева связана с реальным документом;
- для каждого документа автоматически считается число строк;
- для каталогов, на которые ссылается документация, создаются специальные каталоговые узлы с собственными показателями:
  - сколько файлов внутри;
  - сколько строк суммарно внутри;
  - есть ли индексный документ (`README.md` или `index.md`);
- отдельно помечаются документы, которые влияют на корневой `README.md`.

Что это даёт обычному человеку:
- документация перестаёт быть “лесом файлов”, в котором легко потеряться;
- можно быстро увидеть, где находится нужный раздел и как он связан с остальной системой;
- если кто-то меняет важный документ и забывает обновить зависимые, система подаёт сигнал;
- легче понять масштаб изменений: изменился один файл или целый смысловой контур.

Какие проблемы это решает:
- защищает проект от незаметного дрейфа документации;
- делает видимыми скрытые зависимости между документами;
- помогает поддерживать корневой `README.md` в актуальном состоянии;
- позволяет раньше замечать структурные ошибки, например:
  - битые ссылки;
  - каталог без индексного документа;
  - изменение строк в зависимом документе без обновления корня.

Почему это важно именно для Art:
- Art строится не только как код, но и как строго управляемая инженерная программа;
- если документация начинает врать, потом начинают врать чек-листы, тесты и release-решения;
- поэтому дерево документации становится не украшением, а защитным механизмом проекта.

Что делает этот механизм сильнее обычной документации:
- он не только показывает ссылки, но и измеряет структуру;
- он превращает каталоговые ссылки в отдельные контролируемые сущности;
- он автоматически проверяет соответствие line-count реальному состоянию файлов;
- он подаёт сигнал, когда изменение документа может потребовать корректировки зависимых частей проекта.

## 5. Инновационные экспериментальные треки

Эти треки являются частью уникальности Art, но пока должны рассматриваться как controlled experiments.

### 5.1 RTP — Refutation Tournament Protocol
Состояние: `Эксперимент, частично внедрён`

Что это такое:
- система не просто принимает claim, а пытается его оспорить набором refuter-правил.

Что это несёт в проект:
- Art перестаёт быть пассивным отображением доказательств и становится системой, которая активно ищет опровержения;
- это снижает риск ложных решений;
- появляется культура "проверки гипотез на разрушение", а не только на подтверждение.

Для обычного человека:
- это как если бы рядом с каждым важным выводом сидел внутренний критик и пытался доказать, что вывод ошибочен.

### 5.2 LRC — Live Runbook Compiler
Состояние: `Эксперимент, частично внедрён`

Что это такое:
- runbook не просто текст, а набор исполняемых предусловий и шагов, которые можно проверять в реальном времени.

Что это даёт:
- устаревшие инструкции обнаруживаются раньше;
- система может подсказать, чего не хватает для безопасного продолжения;
- инженер меньше полагается на слепую веру в документ.

Для обычного человека:
- это как "умная инструкция", которая сначала смотрит, применим ли вообще текущий шаг к реальной ситуации.

### 5.3 NRAC — No-Regret Action Certificate
Состояние: `Эксперимент, частично внедрён`

Что это такое:
- перед выполнением критического действия система оценивает, насколько оно разумно по сравнению с альтернативами.

Что это даёт:
- меньше риск запустить опасное действие, которое потом придётся откатывать;
- повышается объяснимость автоматизации;
- появляется более взрослый safety-layer для Action Studio.

Для обычного человека:
- это как дополнительный сертификат здравого смысла перед нажатием на опасную кнопку.

### 5.4 Deterministic Incident Twin
Состояние: `Экспериментальная цель, ещё не оформлена как отдельный execution-track`

Что это такое:
- идея воспроизводить инцидент в отдельной безопасной среде так, чтобы результат совпадал с тем, что реально произошло в production.

Что это даёт:
- позволяет разбирать сложные случаи без риска для боевой среды;
- помогает обучать инженеров;
- усиливает доверие к replay и RCA.

### 5.5 Proof-Carrying AI Claims
Состояние: `Экспериментальная цель, частично отражена в laws`

Что это такое:
- AI имеет право выдать claim только тогда, когда этот claim обеспечен evidence и proof chain.

Что это даёт:
- снижает риск опасных и юридически слабых AI-утверждений;
- делает AI не болтливым советчиком, а дисциплинированным участником расследования.

## 6. Утверждённые, но недоведённые концепции

Это самые важные идеи из истории обсуждений, которые нельзя потерять. Они утверждались как сильные differentiators, но пока не получили полного обязательного контура.

### 6.1 Proof Completeness Score
Состояние: `Утверждено, но недоведено`

Смысл:
- у каждого claim должен быть понятный числовой показатель, насколько полна его доказательная база.

Что это дало бы проекту:
- помогло бы быстро понять, насколько вывод опирается на достаточное количество и качество evidence;
- позволило бы различать "слабое предположение" и "хорошо подтверждённый вывод";
- усилило бы Evidence-First количественным измерением.

Почему нельзя терять:
- это один из самых сильных способов сделать расследование не только объяснимым, но и измеримым.

### 6.2 DNA Drift Radar
Состояние: `Утверждено, но недоведено`

Смысл:
- система должна замечать, что в поведении DNA-кластеров появляется новый тревожный паттерн ещё до того, как он разрастётся в полноценный инцидент.

Что это дало бы:
- переход от реактивного режима к проактивному;
- раннее обнаружение новых классов проблем;
- более стратегическое использование Event DNA.

### 6.3 Counterfactual Action Simulator
Состояние: `Утверждено, но недоведено`

Смысл:
- перед действием система должна уметь показать вероятные последствия и альтернативные сценарии.

Что это дало бы:
- меньше необдуманных действий;
- более безопасный Action Studio;
- связь истории прошлых инцидентов с будущими решениями.

### 6.4 Reproducible Incident Capsule
Состояние: `Утверждено, но недоведено`

Смысл:
- инцидент должен собираться в один подписанный, переносимый, проверяемый слепок со snapshot, evidence, policy, audit и окружением.

Что это дало бы:
- огромную ценность для аудита, обучения, регуляторики и повторного расследования;
- удобную форму передачи инцидента между командами;
- ещё более сильный differentiator против обычных observability-решений.

### 6.5 Autonomic UX Governor
Состояние: `Утверждено, но недоведено`

Смысл:
- интерфейс должен сам удерживать usability и p95 latency в рамках бюджета, снижая детализацию и нагрузку по чётким правилам.

Что это дало бы:
- стабильную работу на слабых GPU;
- управляемое поведение Flow/3D режимов;
- меньше жалоб на тормоза и визуальный перегруз.

### 6.6 AST-Driven UI Law Enforcement
Состояние: `Утверждено, но недоведено`

Смысл:
- UI-законы должны проверяться не только тестами после сборки, но и на уровне AST/типов ещё в момент разработки.

Что это дало бы:
- нарушения `tooltip everywhere`, `one-click-to-evidence`, `claim without evidence` ловились бы ещё раньше;
- меньше дорогих runtime-проверок;
- выше дисциплина фронтенда по мере роста экранов.

### 6.7 Самовосстанавливающийся Контур Тестов И Документации
Состояние: `Утверждено, но недоведено`

Смысл:
- система должна уметь автоматически пересобирать или хотя бы полуавтоматически обновлять тесты и docs при изменении контрактов.

Что это дало бы:
- меньше несоответствий между OpenAPI, тестами и README/portal/docs;
- меньше ручной рутины;
- быстрее evolution API и UI.

### 6.8 Брендовый Product Showcase Layer
Состояние: `Утверждено, но недоведено`

Смысл:
- у проекта должен быть не только engineering-ready интерфейс, но и продуманный слой демонстрации: сценарии, анимации, walkthrough и интерактивные витрины.

Что это дало бы:
- продукт легче объяснять клиенту;
- progress виден не только по артефактам CI, но и в наглядном demo-layer;
- усиливается доверие к проекту как к живому продукту, а не как к набору документов.

### 6.9 AI Engineering Operating Model
Состояние: `Утверждено, но недоведено`

Смысл:
- команда ИИ должна работать по формальной модели ролей, обмена знаниями и ретроспективного обучения, а не как случайные отдельные вызовы.

Что это дало бы:
- лучшее качество на длинной дистанции;
- меньше потерь контекста;
- более предсказуемое развитие большого проекта.

## 7. Концепции, которые делают Art особенным как систему

### 7.1 Art не продаёт просто наблюдаемость
Он продаёт:
- доверие к выводам;
- воспроизводимость расследования;
- инженерную дисциплину действий;
- понятное объяснение причин и последствий.

### 7.2 Art связывает несколько редких качеств сразу
Обычно рынок предлагает по отдельности:
- дашборды;
- поиск по логам;
- alerting;
- automation;
- AI-помощника.

Art пытается связать всё это через один общий закон:
- ничего важного без evidence, lineage, audit и управляемого процесса.

### 7.3 Art делает деградацию частью архитектуры
Panel0, fallback, go/no-go, release evidence, platform matrix и stage ladder показывают, что проект думает не только о "счастливом пути", но и о провале.

### 7.4 Art строится как инженерная программа, а не как коллекция wish-list задач
Чек-листы, gates, evidence ledger, observability gaps, risk register и go/no-go превращают замысел в последовательную программу исполнения.

## 8. Что уже есть, а что нельзя потерять дальше

### 8.1 Уже реально закреплено
- Evidence-First
- Event DNA
- Truth Modes
- Panel0
- Investigation Library
- Dialogic Protocol
- Audit + Merkle verify
- Visual Flow как product direction
- RTP/LRC/NRAC как experimental tracks
- semantic tokens
- audio/settings framework
- Linux/Docker/Kubernetes production candidate contour
- операционный визуальный язык тёмно-золотой темы
- truth overlay как отдельный закон дизайн-системы
- settings architecture и profile manager

### 8.2 Требует отдельного remediation, чтобы не потеряться
- Proof Completeness Score
- DNA Drift Radar
- Counterfactual Action Simulator
- Reproducible Incident Capsule
- Autonomic UX Governor
- Deterministic Incident Twin как отдельный execution-track
- Proof-Carrying AI Claims как обязательный adoption path
- AST-driven UI law enforcement
- self-healing tests/docs pipeline
- eBPF evidence linking
- Wasm sandbox для расширяемых действий
- graph-based DNA exploration
- брендовый product showcase layer
- AI engineering operating model

## 9. Дополнительные идеи из истории обсуждений, которые нельзя потерять

Ниже собраны идеи, которые не всегда выглядят как отдельная feature в интерфейсе, но сильно влияют на зрелость проекта.

### 9.1 Общая база знаний для инженерных агентов
Смысл:
- все решения, чек-листы, контракты, runbooks и lessons learned должны складываться в общий контур знаний.

Что это даёт:
- меньше потерь контекста;
- легче синхронизировать документацию и код;
- проще проводить ретроспективы и повторно использовать решения.

### 9.2 Утренний брифинг и ретроспективное обучение агентов
Смысл:
- перед новым циклом работы команда ИИ получает краткую сводку изменений, активных рисков и ближайших целей.

Что это даёт:
- меньше рассинхронизации между агентами;
- меньше повторного изобретения уже принятых решений;
- выше устойчивость длинного проекта.

### 9.3 Интеллектуальное прототипирование интерфейсов по сценариям
Смысл:
- интерфейс можно развивать не только вручную, но и через генерацию UI-скелетов по описанным пользовательским сценариям.

Что это даёт:
- быстрее появляются корректные каркасы экранов;
- легче проверять идеи до тяжёлой ручной полировки;
- можно ускорять развитие новых surface-компонентов без потери laws/gates.

### 9.4 Клиентский интерактивный прототип без установки
Смысл:
- потенциальный заказчик должен иметь возможность увидеть основные поверхности продукта без полной локальной установки.

Что это даёт:
- снижает барьер первого знакомства;
- повышает доверие;
- делает ценность Art более очевидной для нетехнической аудитории.

## 9.5 Контроль полноты по истории обсуждений

После повторной сверки текущего чата и дополнительного файла `чат111` в проект уже перенесены основные крупные линии замысла:
- product concepts и differentiators;
- design/visual language контур;
- AI engineering operating model;
- advanced automation backlog;
- experimental hypotheses и radar.

Что это означает practically:
- ключевые одобренные идеи больше не висят только в сообщениях чата;
- если новая крупная идея не попадает в `PROJECT_HISTORY_AND_CONCEPTS.md`, `revolutionary_hypotheses.md`, `frontier_tech_radar.md` или `ADVANCED_AUTOMATION_BACKLOG.md`, она считается ещё не внесённой в проектовый корпус;
- следующее уплотнение должно идти уже не в сторону “ещё собрать идеи”, а в сторону checklist mapping и реальной реализации.

## 10. Правило дальнейшей работы

Любая новая или ранее утверждённая концепция должна жить не только в чате или идее, а сразу в пяти формах:
1. описание в foundation-контуре;
2. классификация: mandatory / experimental / deferred;
3. checklist mapping;
4. verification path;
5. evidence или явный статус, почему внедрение ещё не завершено.

Если чего-то из этого нет, концепция считается не потерянной, но недовнесённой в проект.

## 11. Заключение

Art уникален не одной функцией, а сочетанием дисциплин:
- доказательность;
- детерминизм;
- управляемое расследование;
- безопасные действия;
- воспроизводимость;
- аварийная устойчивость;
- богатый, но контролируемый интерфейс;
- сильный визуальный язык;
- зрелая автоматизация инженерного контура;
- перспектива глубокой верификации через новые технологические подходы;
- программа исполнения через checklist/gate/evidence.

Именно это делает проект не просто красивой идеей и не просто UI над логами, а попыткой создать инженерную операционную систему для инцидентов.

Но этот замысел требует аккуратности: всё, что было утверждено как ценное и уникальное, должно быть переведено из разговоров и заметок в foundation, чек-листы, тесты и evidence. Этот документ нужен именно для того, чтобы больше ничего из этого корпуса не потерялось.

## 12. APPENDIX: THREAT MODEL FOR ART INCIDENT OS

### 12.1 Назначение, статус и границы честности

Threat Model Appendix является частью исторического канона проекта, а не отдельной “бумагой по безопасности”.

Он нужен для того, чтобы:
- описать классы угроз для `Art Incident OS`;
- явно показать, какие активы и границы доверия мы защищаем;
- связать угрозы с:
  - `observability_gap.*`;
  - runbook-документами;
  - contracts и machine-readable полями;
  - checklist-этапами;
  - CI/test gates;
  - evidence IDs и release priorities.

Этот Appendix состоит из трёх слоёв:
1. `Threat classes` — собственно классы угроз.
2. `Control families` — семейства защитных механизмов.
3. `Readiness / maturity / rollout` — что уже материализовано, что только запланировано, а что остаётся архитектурным горизонтом.

Честная легенда зрелости:
- `materialized` — уже материализовано в коде, документах, runbooks, CI или runtime.
- `planned` — задумано и пришито к проекту, но ещё не завершено как реальный production-contour.
- `architectural_horizon` — стратегическое направление, которое нельзя выдавать за уже внедрённую защиту.
- `missing` — модель угроз требует такую сущность, но в репозитории она пока отсутствует.

Запрещено:
- выдавать `planned` за `materialized`;
- выдавать `architectural_horizon` за production-ready security;
- скрывать `missing`, если модель угроз требует gap/runbook/gate/contract, а их в репозитории нет.

#### Scope (что покрывает Appendix)

Threat Model Appendix покрывает:
- `Art Core`;
- `Art Agent`;
- `Console` и browser surface;
- local stores / local index / spatial store;
- ingest / stream / actions / audit;
- packaging / release / CI/CD;
- backup / restore;
- `InvestigationDoc`, evidence artifacts и audit artifacts;
- future SaaS / multi-tenancy;
- AI / Agent layer;
- crypto / provenance / audit chain.

#### Assumptions (предположения)

Минимальные исходные предположения:
- браузер клиента не считается полностью доверенной средой;
- локальный кэш Console не является `source-of-truth` без подтверждения от `Core`;
- `certified profile` и `general profile` имеют разную жёсткость baseline-защит;
- nat-тесты не равны полной сертификации;
- постквантовая защита (`Post-Quantum`, `PQC`) сейчас является направлением миграции, а не production-claim;
- часть защитных мер в проекте пока materialized только частично и это должно быть отражено честно.

#### Non-goals (что документ не обещает)

Этот Appendix не означает, что проект:
- даёт “100% security guarantee”;
- уже сертифицирован ФСТЭК;
- уже полностью постквантово защищён;
- заменяет собой contracts, runbooks, checklists или CI-gates.

Он нужен как связующий слой между угрозами, защитами, зрелостью и программой исполнения.

### 12.2 Assets (что именно защищаем)

#### 12.2.1 Runtime assets

| Актив | Что это такое простыми словами | C | I | A | Retention horizon |
|---|---|---|---|---|---|
| ingest events | входящие события до и после нормализации | medium | high | high | short / operational |
| normalized events | события в каноническом виде | medium | high | high | medium |
| DNA canonical forms | устойчивые “отпечатки” классов проблем | low | high | high | medium / long |
| DNA cluster identities | идентификаторы и состав кластеров | low | high | high | medium / long |
| replay windows and replay outputs | окна и результаты воспроизведения инцидентов | medium | high | high | medium |
| incident records | карточки инцидентов и их состояние | medium | high | high | long |
| action requests/results | запросы на действия и их результаты | high | very_high | medium | long |
| audit records | append-only журнал подтверждённых действий и событий | high | very_high | high | long |
| Merkle proofs | криптографические доказательства неизменности audit chain | low | very_high | medium | long |
| observability_gap events | сигналы о сломанной наблюдаемости и деградации | low | high | high | medium |
| policy/law versions | версии правил, политик и законов | low | very_high | high | long |
| control-plane commands | управляющие команды runtime-контура | high | very_high | medium | short / long in audit |

#### 12.2.2 Client-side assets

| Актив | Что это такое | C | I | A | Retention horizon |
|---|---|---|---|---|---|
| local cache snapshots | локальные снапшоты в браузере/клиенте | medium | high | medium | short |
| local index | локальный индекс для быстрой навигации | low | high | medium | short |
| spatial store snapshots | локальные spatial/view snapshots | low | medium | medium | short |
| visibility/layout snapshots | состояние пользовательской проекции | low | medium | medium | short |
| InvestigationDoc / incident capsules | переносимые документы расследования | high | very_high | high | long |
| UI policy projection state | локальная проекция того, что UI считает разрешённым | medium | high | medium | short |

#### 12.2.3 Build / release assets

| Актив | Что это такое | C | I | A | Retention horizon |
|---|---|---|---|---|---|
| source provenance | доказательство происхождения исходников | low | very_high | medium | long |
| dependency graph | граф зависимостей | low | high | medium | medium |
| lockfiles | зафиксированные версии зависимостей | low | very_high | medium | medium |
| toolchain pinning state | зафиксированное состояние toolchain | low | very_high | medium | medium |
| build provenance / attestations | подтверждения сборки | low | very_high | medium | long |
| SBOM | описание состава release | low | high | medium | long |
| release artifacts | итоговые бинарники/пакеты | medium | very_high | high | long |
| package metadata | метаданные пакетов и manifests | low | high | medium | medium |
| signatures and checksums | подписи и контрольные суммы | low | very_high | medium | long |

#### 12.2.4 Long-term / legal / audit assets

| Актив | Что это такое | C | I | A | Retention horizon |
|---|---|---|---|---|---|
| archived investigations | архивы расследований | high | very_high | medium | long |
| evidence bundles | собранные пакеты доказательств | high | very_high | medium | long |
| backup sets | backup-наборы storage | high | very_high | high | long |
| restore procedures and drill reports | процедуры и результаты тренировок восстановления | medium | high | high | long |
| cryptographic metadata for long-term verification | метаданные криптографической проверяемости | low | very_high | medium | long |

### 12.3 Trust Boundaries (границы доверия)

| Граница доверия | Trust level | Почему это критично | Обязательные controls | Evidence / CI gates |
|---|---|---|---|---|
| Internet / edge -> Core ingest | untrusted | внешняя сеть может давить ingress и подделывать контекст | ingress shield, rate limiting, quotas, payload validation | `check_protective_contours.sh`, stage12/stage24/stage37 gates |
| Agent -> Core | partially_trusted | агент наш, но его среда может быть скомпрометирована | enrollment, identity, transport trust, replay/backlog validation | stage17/stage18/stage37 |
| Console browser -> Core API | untrusted | браузер нельзя считать доверенным security-source | trust boundary hardening, browser policy, fail-closed actor-context | stage10/stage15/stage16/stage24 |
| Console local stores -> Core truth | partially_trusted | локальный state удобен, но не источник истины | stale-data markers, sync law, truth modes | stage28/stage35 |
| CI/build environment -> artifact registry | partially_trusted | сборка может быть скомпрометирована | provenance, signing, SBOM, branch protection | stage04/release gates |
| Artifact registry -> deployment target | partially_trusted | подмена или downgrade release | verify signatures/checksums/provenance | stage04/stage24/stage37 |
| Tenant A <-> Tenant B | untrusted | cross-tenant leakage разрушает SaaS модель | tenant isolation, quotas, crypto separation | stage36, future tenant gates |
| AI/model artifacts -> inference runtime | partially_trusted | модель и контекст могут быть испорчены или не теми | model provenance, output validation, tool boundary | planned / future AI contours |
| Backup store -> restore pipeline | partially_trusted | backup может быть битым, украденным или подменённым | integrity checks, drill restore, immutability | stage11/stage24 |
| Admin/operator surfaces -> runtime control plane | partially_trusted | privileged человек тоже может ошибаться или злоупотребить | RBAC, audit, secure actions, no-regret path | stage15/stage33 |

### 12.4 Threat Actors (кто атакует)

| Актор | Motivation | Typical access level | Likely attack surface | Risk priority |
|---|---|---|---|---|
| внешний сетевой атакующий | отказ в обслуживании, exploitation, reconnaissance | internet-facing only | edge, browser, API, flood | high |
| злоумышленный tenant | получить больше ресурсов или чужие данные | tenant-scoped | SaaS APIs, quotas, context bleed | high |
| скомпрометированный tenant | unintentionally hostile | tenant-scoped + stolen credentials | noisy-neighbor, data leakage | high |
| скомпрометированный agent/source | подделка telemetry/evidence | host-level in one segment | agent transport, receivers, source poisoning | high |
| malicious dependency publisher | supply-chain compromise | external ecosystem | crates, npm, GitHub Actions | high |
| compromised CI/build environment | artifact tampering | build-system level | release pipeline, provenance, secrets | high |
| insider operator/admin/support engineer | misuse or bypass of controls | privileged human | actions, support tools, audit | high |
| accidental buggy integrator | случайная поломка протокола | integrator-level | bad payloads, schema drift, loops | high |
| passive long-term traffic collector | future crypto break / metadata mining | network visibility | long-lived traffic, archives | medium / horizon |
| malicious prompt/data injector | навязать опасный AI-context | data-only | logs, runbooks, incident text, retrieval | high |
| model artifact tamperer | подмена веса/модели/конфигурации | supply-chain / model registry | AI layer | horizon |

### 12.5 Threat Domains (карта доменов угроз)

| Домен | Почему критичен для Art | Какие assets затрагивает | Какие trust boundaries пересекает |
|---|---|---|---|
| Supply Chain | Art живёт на proofs, release trust и reproducibility | source, lockfiles, SBOM, artifacts | CI -> registry -> deployment |
| Identity & Secrets | без идентичностей нельзя доверять agent, release и admin actions | keys, certs, identities, tokens | Agent -> Core, operator -> control plane |
| Control Plane (Actions) | опасные действия в инциденте не имеют права выполняться “вслепую” | action requests/results, audit, policy versions | operator -> runtime |
| Data Plane (Ingest / Stream) | основной вход hostile-трафика и “медленного” зашумления | ingest events, normalized events, queues | Internet/Agent -> Core |
| Storage | SQLite, backups, WAL, local stores — фундамент доказательности | incidents, audits, evidence, backups | Core storage, backup store |
| Console / Client | ложная проекция и browser attacks обманывают оператора | local cache, UI policy, exports | browser -> Core |
| Multi-tenancy / SaaS | при расширении в SaaS cross-tenant leakage становится критическим | tenant data, keys, caches | Tenant A <-> Tenant B |
| AI / Agent | AI может навредить не злым умыслом, а невалидным контекстом и unsafe output | prompts, retrieval, AI outputs | model/runtime/tool boundaries |
| Time & Ordering | Art строится на replay, audit chronology и sequence semantics | replay windows, audit chain, seq state | source time -> core time |
| Config & Policy Drift | даже “правильная” система ломается от дрейфа политики | policy versions, hardening settings | all administrative boundaries |
| Backup & Restore | без этого нет реальной живучести и evidence continuity | backups, drill reports, restore procedures | backup store -> restore |
| Post-Quantum / Crypto Agility | long-lived evidence должен пережить смену криптографической эпохи | signatures, archives, long-term metadata | network/archive/verification horizon |

### 12.6 Threat Catalog

#### 12.6.1 Supply Chain

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-SC-01 | threat | tampered source / branch policy bypass | protected branches, reviews, CODEOWNERS, verified commits | repo protection evidence, stage01/stage04 | materialized |
| TM-SC-02 | threat | dependency confusion / typosquatting / unpinned transitive drift | lockfiles, dependabot policy, SCA, pinning | stage04 `sca`, `sdlc-gate` | materialized |
| TM-SC-03 | threat | compromised CI runner or build drift | reproducible build, provenance, signed artifacts | provenance checks, release gates | planned |
| TM-SC-04 | threat | unsigned artifact / checksum mismatch / tampered SBOM | signatures, checksums, provenance verify | `observability_gap.release_signing_failed` | materialized |
| TM-SC-05 | readiness-risk | downgrade to signed-but-vulnerable artifact / revoked artifact still installable | release channel policy, revocation, version skew policy | currently only partial release policy | planned |

#### 12.6.2 Identity & Secrets

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-ID-01 | threat | unknown or spoofed service / agent identity | enrollment, mTLS, attestation | missing full runtime contour | planned |
| TM-ID-02 | threat | admin impersonation / support misuse / break-glass abuse | RBAC, audit, secure actions | audit chain, stage15/stage33 | materialized |
| TM-ID-03 | threat | signing identity compromise | provenance and verify baseline | release signing gates | materialized |
| TM-ID-04 | readiness-risk | rotation / revocation overdue | certificate/key lifecycle | `observability_gap.key_rotation_overdue` | missing |
| TM-ID-05 | future-risk | tenant key reuse / weak KMS boundary | per-tenant crypto isolation | SaaS future controls | architectural_horizon |

#### 12.6.3 Control Plane (Actions)

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-CP-01 | threat | unauthorized action execution | RBAC, preflight-first, policy-as-ui | `observability_gap.action_preflight_missing` | materialized |
| TM-CP-02 | threat | replayed or forged action result | lineage, audit chain, secure protocol | stage33 actions protocol | planned |
| TM-CP-03 | threat | schema-valid but harmful parameters / target confusion | safe actions protocol, NRAC/no-regret contour | secure actions tests | planned |
| TM-CP-04 | control-failure | policy engine unavailable / audit write failed / signature unavailable | fail closed or safe-noop | must remain explicit in runbooks | planned |

#### 12.6.4 Data Plane (Ingest / Stream)

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-DP-01 | threat | volumetric overload / burst / queue exhaustion | backpressure, ingress shield, quotas | `observability_gap.ingest_overloaded`, `ddos_suspected` | materialized |
| TM-DP-02 | threat | slow-burn overload that legally fills storage | storage watermarks, retention as protective control | `storage_pressure_high` | planned |
| TM-DP-03 | threat | evidence poisoning / DNA poisoning | trusted source law, canonicalization discipline | DNA mismatch/drift gaps | planned |
| TM-DP-04 | threat | parser bombs / nesting / compression abuse | size limits, parser validation | payload-too-large, parse errors | planned |
| TM-DP-05 | threat | noisy source / priority inversion / starvation | per-source quotas, queue integrity | `observability_gap.queue_integrity_violation`, `noisy_neighbor_quota_violation` | missing |
| TM-DP-06 | threat | duplicate keys / encoding ambiguity / version skew | canonicalization and schema discipline | `observability_gap.event_order_violation` | missing |

#### 12.6.5 Storage

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-ST-01 | threat | disk full / reserve exhausted / WAL growth | watermarks, write shed, backups, vacuum | `observability_gap.storage_disk_full`, `storage_pressure_high`, `storage_wal_growth_anomaly` | planned |
| TM-ST-02 | threat | corruption of DB/WAL | backup/restore, integrity checks, read-only fallback | `observability_gap.storage_corrupted`, `storage_read_only` | materialized |
| TM-ST-03 | threat | local snapshot forgery / cache divergence | truth modes, core-confirmed truth | no dedicated gap for all client variants | planned |
| TM-ST-04 | threat | backup corruption / tampering / confidentiality breach | restore drills, backup integrity checks, immutability strategy | `backup_integrity_failed`, `restore_integrity_failed` | missing |
| TM-ST-05 | threat | inode exhaustion / backup growth pressure | quota and archive controls | no explicit gap yet | missing |

#### 12.6.6 Console / Client

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-CL-01 | threat | XSS / unsafe evidence rendering / CSP bypass | browser surface hardening | `observability_gap.browser_surface_policy_degraded` | planned |
| TM-CL-02 | threat | local state tampering / stale policy projection | connected system visibility, truth law, stale markers | partial runtime materialization | planned |
| TM-CL-03 | threat | token/session misuse / CSRF-like misuse | auth boundary, trusted proxy rules | no fully explicit client gap set | planned |
| TM-CL-04 | threat | malicious extensions / clipboard leakage / unsafe downloads | browser policy and export policy | mostly documented, not fully gated | planned |
| TM-CL-05 | threat | GPU/render overload / scene complexity explosion | flow downgrade, perf budgets | `observability_gap.flow_visual_overload`, `spatial_index_degraded` | materialized |

#### 12.6.7 Multi-tenancy / SaaS

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-SAAS-01 | threat | cross-tenant leakage | tenant isolation contracts, quotas | `observability_gap.saas_tenant_isolation_failed`, `tenant_isolation_breach` | planned |
| TM-SAAS-02 | threat | noisy neighbor | per-tenant quota/budget/isolation | `observability_gap.noisy_neighbor_quota_violation` | missing |
| TM-SAAS-03 | threat | support/admin leakage | audit, scoped support tools | current control partial | planned |
| TM-SAAS-04 | future-risk | tenant-aware crypto isolation and AI-context isolation | per-tenant crypto and retrieval separation | roadmap only | architectural_horizon |

#### 12.6.8 AI / Agent

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-AI-01 | threat | prompt injection through incident data, logs, runbooks | separated suggestion vs execution, evidence-first | AI-specific runtime contour not finished | planned |
| TM-AI-02 | threat | retrieval poisoning / ranking corruption | evidence provenance, retrieval controls | `model_provenance_mismatch` / retrieval-specific gap | missing |
| TM-AI-03 | threat | schema-valid but semantically dangerous AI output | output validators, trusted tool boundaries | `agent_output_schema_violation` | missing |
| TM-AI-04 | future-risk | wrong/unsigned model or silent downgrade | model provenance/signing | horizon | architectural_horizon |
| TM-AI-05 | threat | tool abuse / hidden escalation | secure actions + AI boundary | planned AI-action contour | planned |

#### 12.6.9 Time & Ordering

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-TM-01 | threat | source time drift / skewed clocks | explicit clock domains, time sanity checks | `observability_gap.time_sync_drift` | missing |
| TM-TM-02 | threat | duplicate delivery / reorder / seq gaps | monotonic seq guards, replay integrity | `observability_gap.event_order_violation`, `queue_integrity_violation` | planned |
| TM-TM-03 | threat | broken replay chronology / false audit narrative | replay windows, Merkle chronology | DNA/audit replay tests | planned |

#### 12.6.10 Config & Policy Drift

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-DR-01 | threat | security config drift | hardening baselines, startup validator | `observability_gap.hardening_profile_mismatch` | missing |
| TM-DR-02 | threat | policy drift | policy versions, root sync, checklist sync | `observability_gap.policy_drift_detected` | missing |
| TM-DR-03 | threat | UI/backend policy projection drift | UI truth laws, connected-system visibility | partial UI guard set | planned |

#### 12.6.11 Backup & Restore

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-BR-01 | threat | corrupted/tampered backup | backup integrity verify | `backup_integrity_failed` | missing |
| TM-BR-02 | threat | restore process broken or requires improvisation | drills, restore runbooks, evidence | `restore_integrity_failed` | missing |
| TM-BR-03 | threat | leaked or unencrypted backup | confidentiality policy, crypto policy | backup confidentiality contour partial | planned |
| TM-BR-04 | future-risk | mutable-only backup tier / ransomware | immutable backup tier | horizon | architectural_horizon |

#### 12.6.12 Post-Quantum / Crypto Agility

| ID | Type | Title | Preventive controls | Detection / gaps | Status |
|---|---|---|---|---|---|
| TM-PQ-01 | future-risk | long-lived signatures become weak over time | crypto agility metadata, re-sign strategy | `crypto_agility_gap` | missing |
| TM-PQ-02 | future-risk | deprecated signature algorithms remain in use | algorithm lifecycle policy | `deprecated_signature_algorithm_in_use` | missing |
| TM-PQ-03 | future-risk | no hybrid migration path | hybrid transition design | documented only in concept layer | architectural_horizon |

### 12.7 Control Families

| Control family | Что это такое простыми словами | Домены | Status |
|---|---|---|---|
| Build provenance & signing | доказательство, что релиз собран из правильного исходника и не подменён | Supply Chain | materialized |
| Identity / key / certificate lifecycle | управление идентичностями, сертификатами и ключами | Identity & Secrets | planned |
| Access control / capability model | кто что может делать и почему | Control Plane, SaaS | materialized |
| Schema validation / parser hardening | защита от вредных payload и неоднозначного разбора | Data Plane | planned |
| Rate limiting / quotas / backpressure | контролируемое ограничение нагрузки | Data Plane, SaaS | materialized |
| Storage pressure protection | защита от медленного заполнения БД и диска | Storage | planned |
| Drift detection / baseline enforcement | защита от расхождения правил, конфигов и документов | Config & Policy Drift | planned |
| Audit immutability / Merkle verification | доказуемая неизменность audit chain | Control Plane, Time & Ordering | materialized |
| Backup / restore assurance | уверенность, что backup есть, а restore реально работает | Storage, Backup & Restore | planned |
| Tenant isolation | отделение tenant-ов друг от друга | SaaS | planned |
| AI output / provenance controls | защита от unsafe AI-output и подмены модели | AI / Agent | planned |
| Crypto agility / algorithm lifecycle | готовность менять криптографические алгоритмы без потери совместимости | Post-Quantum / Crypto Agility | architectural_horizon |

### 12.8 Observability Gap Matrix

| gap_id | covered_threat_ids | status | Что сейчас есть |
|---|---|---|---|
| observability_gap.storage_near_full | TM-ST-01 | missing | нужен отдельный early warning до critical watermark |
| observability_gap.storage_quota_exceeded | TM-ST-01 | missing | нужен отдельный quota breach event |
| observability_gap.storage_wal_growth_anomaly | TM-ST-01 | missing | WAL anomaly требуется моделью, но ещё не материализован |
| observability_gap.backup_integrity_failed | TM-ST-04, TM-BR-01 | missing | backup integrity proof пока не вынесен в отдельный gap |
| observability_gap.restore_integrity_failed | TM-ST-04, TM-BR-02 | missing | нужен отдельный restore failure integrity event |
| observability_gap.identity_attestation_failed | TM-ID-01 | missing | agent/service attestation contour ещё не завершён |
| observability_gap.key_rotation_overdue | TM-ID-04 | missing | lifecycle gap пока отсутствует |
| observability_gap.signing_provenance_gap | TM-SC-03, TM-ID-03 | missing | release/provenance есть, отдельного gap нет |
| observability_gap.time_sync_drift | TM-TM-01 | missing | требуется для time/order safety |
| observability_gap.event_order_violation | TM-DP-06, TM-TM-02 | missing | очередь и порядок пока покрыты косвенно |
| observability_gap.policy_drift_detected | TM-DR-02 | missing | drift law есть, отдельного gap нет |
| observability_gap.hardening_profile_mismatch | TM-DR-01 | missing | hardening drift ещё не выделен отдельным event |
| observability_gap.tenant_isolation_breach | TM-SAAS-01 | missing | есть близкий `saas_tenant_isolation_failed`, но не этот canonical alias |
| observability_gap.noisy_neighbor_quota_violation | TM-DP-05, TM-SAAS-02 | missing | нужен для SaaS hostile budget |
| observability_gap.model_provenance_mismatch | TM-AI-02, TM-AI-04 | missing | AI/model layer ещё не материализован полностью |
| observability_gap.agent_output_schema_violation | TM-AI-03 | missing | нужен для AI output integrity |
| observability_gap.crypto_agility_gap | TM-PQ-01 | missing | нужен как early maturity signal |
| observability_gap.deprecated_signature_algorithm_in_use | TM-PQ-02 | missing | нужен для algorithm lifecycle |

Уже materialized и используемые gap-события, на которые опирается текущая модель:
- `observability_gap.storage_disk_full`
- `observability_gap.storage_pressure_high`
- `observability_gap.storage_corrupted`
- `observability_gap.storage_read_only`
- `observability_gap.ddos_suspected`
- `observability_gap.ingress_shield_degraded`
- `observability_gap.unsafe_startup_config_refused`
- `observability_gap.queue_integrity_violation`
- `observability_gap.guard_self_test_failed`
- `observability_gap.trust_boundary_violation`
- `observability_gap.browser_surface_policy_degraded`
- `observability_gap.connected_system_not_visible`
- `observability_gap.connected_system_coverage_drift`

### 12.9 Maturity Model

| Family | documented | contracted | implemented | tested | gated_in_ci | drill_exercised | production_enforced |
|---|---|---|---|---|---|---|---|
| Supply chain provenance | yes | yes | yes | yes | yes | partial | partial |
| Storage corruption recovery | yes | yes | yes | yes | partial | yes | partial |
| Storage pressure / quota | yes | partial | partial | partial | partial | no | no |
| Trust boundary hardening | yes | yes | partial | partial | yes | no | partial |
| Browser surface hardening | yes | yes | partial | partial | yes | no | partial |
| Backup / restore integrity | yes | partial | partial | partial | partial | partial | partial |
| SaaS tenant isolation | yes | yes | partial | partial | partial | no | no |
| AI output / provenance | yes | partial | no | no | no | no | no |
| Time / ordering anomaly handling | yes | partial | partial | partial | partial | no | no |
| Crypto agility / PQC horizon | yes | no | no | no | no | no | no |

### 12.10 Mapping Section

#### 12.10.1 Threat <-> Contracts

Эта модель угроз требует или подтверждает необходимость таких contract-fields и schema family:
- `tenant_id` и tenant-scoping metadata everywhere, где появится multi-tenancy;
- `truth_mode`, lineage и evidence metadata;
- `signature_algorithm`, `crypto_profile_version`, provenance metadata;
- replay / sequence / ordering metadata;
- policy versioning fields;
- connected-system coverage projection fields:
  - `declared_data_kinds`;
  - `observed_data_kinds`;
  - `receiver_kinds`;
  - `telemetry_endpoints`.

#### 12.10.2 Threat <-> Runbooks

Materialized threat families уже связаны с runbooks:
- `ddos_suspected`
- `ingress_shield_degraded`
- `trust_boundary_violation`
- `browser_surface_policy_degraded`
- `storage_corruption_runbook`
- `storage_pressure_high`
- `connected_system_not_visible`
- `unsafe_startup_config_refused`
- `queue_integrity_violation`
- `guard_self_test_failed`

Missing runbooks, которые модель требует как следующий шаг:
- backup integrity failure;
- restore integrity failure;
- key rotation overdue;
- time sync drift;
- tenant isolation breach canonical alias;
- AI/model provenance mismatch;
- crypto agility gap.

#### 12.10.3 Threat <-> Checklists

Ключевые соответствия:
- supply chain -> stage04, stage24, stage37;
- storage pressure / storage corruption -> stage11, stage12, stage24, stage37;
- trust boundary -> stage15, stage24, stage33, stage37;
- browser hardening -> stage10, stage16, stage24, stage28, stage37, stage40;
- backup/restore -> stage11, stage24;
- SaaS isolation -> stage36;
- AI provenance / output -> continuation stages 39, 42, 43, 44, 45;
- connected system visibility -> stage18, stage19, stage20, stage28;
- REGART hostile harness -> stage05, stage06, stage20, stage24, stage38.

#### 12.10.4 Threat <-> Evidence IDs

Уже существующие evidence families:
- release evidence ledger;
- stage11 hostile recovery logs;
- stage37 Linux/runtime proofs;
- stage24 GO/NO-GO and release docs;
- pinned REGART harness checks.

Недостающие evidence families, которые Appendix делает явными требованиями:
- backup integrity drill evidence;
- restore integrity drill evidence;
- time/order anomaly drill evidence;
- tenant noisy-neighbor hostile evidence;
- AI/model provenance evidence;
- crypto-agility migration evidence.

### 12.11 Rollout Priorities

#### 12.11.1 Release blockers (до ближайшего release-grade baseline)
- storage pressure contour;
- edge admission / ingress perimeter layer;
- SQLite hardening baseline + compatibility tests;
- systemd hardening baseline;
- config/policy drift detection;
- trust boundary hardening;
- browser surface hardening.

#### 12.11.2 Near-term roadmap
- identity baseline for agents and services;
- tenant quotas / isolation;
- restore drills;
- AI output validation;
- time/order anomaly handling;
- parser fuzzing and ambiguity handling.

#### 12.11.3 Architectural horizon
- PQC migration machinery;
- deeper KMS/HSM integration;
- immutable backup tier;
- stronger tenant isolation pools;
- stronger admin/support isolation;
- full AI/model provenance chain.

### 12.12 Threat Model Change Log

Эта редакция Appendix впервые formalizes:
- ingress/perimeter anti-DDoS как отдельный threat/control family;
- trust boundary hardening как самостоятельный security contour;
- browser surface hardening как полноценный protection domain;
- connected system visibility как operator-truth control, а не просто UI-функцию;
- storage pressure / startup validator / queue integrity / guard self-observability как protective safeguards families.

В этой редакции:
- часть controls переведена из “идеи” в `materialized` или `planned` с честной пометкой;
- впервые явно перечислены `missing observability_gap.*`, требуемые моделью угроз;
- maturity model встроена в исторический документ и связана со зрелостью всего проекта, а не только security-слоя.
